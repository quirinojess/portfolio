export interface Theme {
 colors: {
  darkDeep: string;
  darkMedium: string;
  primary: string;
  secondary: string;
 };
 fonts: {
  primary: string;
 };
 spacing: {
  small: string;
  medium: string;
  large: string;
 };
}
